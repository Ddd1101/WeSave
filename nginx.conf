server {
    listen 80;
    server_name localhost;

    # 前端静态文件
    root /root/workplace_shop/WeSave/Page/dist;
    index index.html;

    # 前端路由
    location / {
        try_files $uri $uri/ /index.html;
    }
}
